{
  "dependencies": {
    "three": "^0.174.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r assets/* dist/assets",
    "preview": "vite preview"
  }
}
